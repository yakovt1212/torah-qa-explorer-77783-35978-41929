# בדיקה ותיקון: יקוק → יהוה

## תאריך: 19 אוקטובר 2025

## בדיקה מקיפה:

### תוצאות הבדיקה:
✅ **אין מופעים של "יקוק" כשם ה' בכל הפרויקט**

### מה בדקנו:
1. ✅ כל קבצי ה-JSON בתיקיית `src/data/`
2. ✅ כל קבצי TypeScript/TSX בפרויקט
3. ✅ קוד ה-TypeScript והקומפוננטות

### ממצאים:
- **במקום `bereishit.json`**: נמצא "זיקוק" (חלק מהמילה "מרוב זיקוק") - **לא צריך לשנות**
- **במקום `shemot.json`**: נמצא "חיקוק" (חלק מהמילה "לשון חיקוק") - **לא צריך לשנות**
- **קובץ `fixData.ts`**: כבר קיים קוד המטפל בהמרה

### שיפור שבוצע:

#### קובץ: `src/utils/fixData.ts`
**שינוי**: שיפרתי את ה-regex להמרה חכמה יותר:

```typescript
// Before:
fixed = fixed.replace(/יקוק/g, 'יהוה');

// After:
fixed = fixed.replace(/(\s|"|'|^)(יקוק)(\s|'|"|,|\.|\?|!|:|;|$)/g, '$1יהוה$3');
```

**הבדל**:
- ✅ ה-regex החדש מחליף **רק** את "יקוק" כשהוא מילה עצמאית
- ✅ לא יחליף "יקוק" שהוא חלק ממילה אחרת (כמו "חיקוק", "זיקוק")
- ✅ מטפל בכל סימני הפיסוק: רווחים, מירכאות, פסיקים, נקודות וכו'

## איך זה עובד במערכת:

### הפונקציה `fixJsonContent`:
```typescript
export const fixJsonContent = (content: string): string => {
  // מחליף "אבן עזרה" ל"אבן עזרא"
  let fixed = content.replace(/אבן עזרה/g, 'אבן עזרא');
  
  // מחליף "יקוק" ל"יהוה" (רק כמילה עצמאית)
  fixed = fixed.replace(/(\s|"|'|^)(יקוק)(\s|'|"|,|\.|\?|!|:|;|$)/g, '$1יהוה$3');
  
  return fixed;
};
```

### היכן הפונקציה משמשת:
1. ✅ **`src/pages/Index.tsx`** - בפונקציה `fixSefer()` שמתקנת את כל הנתונים בטעינה
2. ✅ **`src/hooks/useLazySearch.ts`** - בחיפוש עצלן (lazy search)

### תהליך התיקון:
```
קריאת JSON → fixJsonContent() → המרת יקוק→יהוה → שימוש בנתונים מתוקנים
```

## סיכום:
✅ **הכל תקין!** אין מופעים של "יקוק" כשם ה' בפרויקט  
✅ הקוד שיפרר להתמודד נכון עם מילים דומות  
✅ המערכת מוכנה למקרה שיתווספו נתונים חדשים עם "יקוק"  
✅ כל הנתונים מטופלים אוטומטית בטעינה  

## דוגמאות לעבודה נכונה:
- ✅ " יקוק אמר" → " יהוה אמר"
- ✅ "יקוק'" → "יהוה'"
- ✅ "מרוב זיקוק" → "מרוב זיקוק" (לא משתנה!)
- ✅ "לשון חיקוק" → "לשון חיקוק" (לא משתנה!)
